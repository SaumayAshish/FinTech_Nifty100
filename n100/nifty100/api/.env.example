# Django
DJANGO_SECRET_KEY=change-me
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# API is public read-only (no auth); this caps requests per anonymous client
API_ANON_THROTTLE_RATE=120/min

# Database (set USE_POSTGRES=true to use Postgres instead of the sqlite fallback)
USE_POSTGRES=false
DB_NAME=nifty100_dw
DB_USER=postgres
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# create_admin.py
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me
